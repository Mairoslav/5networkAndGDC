//: [Previous](@previous)

import Foundation

// MARK: 3. URLComponents

/*
 00:00 OK so we saw that with Swift URL struct, we can create an URL from a String and parse out (print out) its different components. But in addition to deconstracting different parts, we would like to be able to construct a URL from components that make it up like its scheme, user ... and also *** query and fragment (the last two we could not add properly - check previous lesson)
 
 00:20 *** Fortunately, Swift has covered this with a second struct called URL components. Let's see how it works.
 */

// 00:25 We can start a new Playgound page to explore URL components - check next Playground page called 

//: [Next](@next)
